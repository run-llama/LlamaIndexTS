{
  "$schema": "https://turbo.build/schema.json",
  "globalEnv": ["PINECONE_CHUNK_SIZE", "PINECONE_INDEX_NAME"],
  "globalDependencies": ["**/.env.*local"],
  "pipeline": {
    "build": {
      "dependsOn": ["^build"],
      "outputs": ["dist/**", "build/**"]
    },
    "lint": {},
    "test": {},
    "dev": {
      "cache": false,
      "persistent": true
    }
  }
}
